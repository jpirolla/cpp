#include <iostream>

using namespace std; 

// tipo bool já esta incluso na biblioteca padrao do C++, ao contrario de C
// 0 for false and 1 for true

int main(){

	// bool var = 3 < 2;
	// bool var = 3 >= 3;
	//bool var = 4 == 2 * 2;
	//bool var = true && false;
	// bool var = true || false;
	
	bool var = true + false;

	cout << var << endl;

	return 0;
}